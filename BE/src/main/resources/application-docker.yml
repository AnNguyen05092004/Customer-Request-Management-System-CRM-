spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  flyway:
    locations: classpath:db/migration,classpath:db/demo

crm:
  security:
    jwt:
      secret: ${JWT_SECRET}
