spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/bzcom_crm}
    username: ${DB_USERNAME:bzcom}
    password: ${DB_PASSWORD:bzcom_local_password}
  flyway:
    locations: classpath:db/migration,classpath:db/demo

crm:
  security:
    jwt:
      secret: ${JWT_SECRET:local-development-secret-change-before-production-123456}
